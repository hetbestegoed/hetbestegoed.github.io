{
  "name": "tailwind",
  "scripts": {
    "start": "npx http-server .",
    "css": "npx tailwindcss -i ./base.css -o ./dist/output.css --watch",
    "css:prod": "npx tailwindcss -i ./base.css -o ./dist/output.css --optimize",
    "build:prod": "npm run css:prod && cp index.html dist/ && cp -R avond-4daagse-elst dist/ && cp -R public dist/"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.0.3",
    "tailwindcss": "^3.3.5"
  }
}
